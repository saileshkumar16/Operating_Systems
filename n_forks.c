// Online C compiler to run C program online
#include <stdio.h>
#include <unistd.h>

int main() 
{
    
    
    fork(); // will create exact duplicate and point to next line, 
    fork(); // will create exact duplicate and iteratte it back, total if n forks 2 power n processes will be created
   
    printf("Hi");

    return 0;
}